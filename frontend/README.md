# AlkebulanX Frontend

This is the React/Vite frontend for AlkebulanX.

For full setup, environment variables, backend instructions, and deployment notes, see the root `README.md`.

## Commands

```powershell
npm install
npm run dev
npm run lint
npm run build
npm run preview
```
